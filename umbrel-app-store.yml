id: "rwh-ks" # Choose the ID for your app store. This should contain only alphabets ("a to z") and dashes ("-").
name: "RWHKS" # Choose the name of your app store. It will show up in the UI as "<name> App Store".

apps:
  - id: rwh-ks-public-pool-bch-solo
    path: apps/rwh4ks-public-pool-bch-solo
